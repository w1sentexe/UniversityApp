name: Build and Deploy

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  REGISTRY: ghcr.io
  BACKEND_IMAGE: w1sentexe/backend

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run pre-commit hooks
        uses: pre-commit/action@v3.0.1

  build_and_push:
    needs: lint
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      backend_changed: ${{ steps.filter.outputs.backend }}
      nginx_changed: ${{ steps.filter.outputs.nginx }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Смотрит diff последнего пуша и решает, что реально изменилось.
      # Все шаги ниже условно пропускаются, если backend не затронут —
      # именно здесь и решается "пересобирать или нет".
      - name: Detect changed paths
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend:
              - 'app/**'
              - 'Dockerfile'
              - 'pyproject.toml'
              - 'uv.lock'
            nginx:
              - 'nginx/**'

      - name: Log in to GHCR
        if: steps.filter.outputs.backend == 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        if: steps.filter.outputs.backend == 'true'
        uses: docker/setup-buildx-action@v3

      # 1.0.x, где x = run_number - 1 (первый успешный билд получает 1.0.0)
      - name: Compute version tag
        if: steps.filter.outputs.backend == 'true'
        id: version
        run: echo "value=1.0.$(( ${{ github.run_number }} - 1 ))" >> "$GITHUB_OUTPUT"

      - name: Build and push backend image
        if: steps.filter.outputs.backend == 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.BACKEND_IMAGE }}:latest
            ${{ env.REGISTRY }}/${{ env.BACKEND_IMAGE }}:${{ steps.version.outputs.value }}
          cache-from: type=gha,scope=backend
          cache-to: type=gha,mode=max,scope=backend

  deploy:
    needs: build_and_push
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    steps:
      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_SSH_PORT }}
          script: |
            set -e
            cd ${{ secrets.VPS_PROJECT_PATH }}

            # Синхронизируем код (nginx.conf, front/, docker-compose.prod.yml).
            # Выполняется всегда — front-статика на диске должна быть свежей
            # независимо от того, что именно менялось.
            git fetch origin main
            git reset --hard origin/main

            if [ "${{ needs.build_and_push.outputs.backend_changed }}" = "true" ]; then
              echo "==> Backend changed, pulling new image and restarting app"
              echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
              docker compose -f docker-compose.prod.yml pull app
              docker compose -f docker-compose.prod.yml up -d --no-deps app
            else
              echo "==> Backend unchanged, skipping app restart"
            fi

            if [ "${{ needs.build_and_push.outputs.nginx_changed }}" = "true" ]; then
              echo "==> nginx config changed, recreating container"
              docker compose -f docker-compose.prod.yml up -d --force-recreate --no-deps nginx
            else
              echo "==> nginx config unchanged, skipping restart"
            fi

            # front/ обновляется просто через git reset выше — не требует
            # ни пересборки, ни reload (volume читается заново на каждый запрос)

            docker image prune -f
